{
  "_README": [
    "SHAPE ONLY. Every value below is deliberately fake and will be REFUSED when",
    "APP_ENV=production, because they read as placeholders.",
    "",
    "Do NOT copy this to backend/seed_api_credentials.json and fill it in — this",
    "repository is PUBLIC, and the seeder refuses to read any credentials file that",
    "git tracks. Put the real file somewhere outside the repo instead:",
    "",
    "  SEED_API_CREDENTIALS_FILE=/run/secrets/api_credentials.json \\",
    "    python -m app.db.seed_api_credentials",
    "",
    "Or skip the file entirely and use environment variables, which is what a",
    "container deployment wants anyway — SEED_CRED_<SLUG>_<FIELD_KEY>:",
    "",
    "  SEED_CRED_ANTHROPIC_API_KEY, SEED_CRED_GOOGLE_CLIENT_SECRET,",
    "  SEED_CRED_MAIL_PASSWORD, SEED_CRED_SLACK_WEBHOOK_URL, ...",
    "",
    "An environment variable wins over this file for the same field.",
    "Omit any field you do not want to change: a field that is not supplied is left",
    "exactly as it is, so re-running to rotate one secret leaves the others alone.",
    "Omit a whole provider and it is skipped — no half-configured row appears.",
    "",
    "Keys not declared by a provider are ignored and reported, so a typo shows up",
    "instead of silently doing nothing. Run --dry-run first; it prints field KEYS",
    "and never values."
  ],

  "google": {
    "client_id": "changeme.apps.googleusercontent.com",
    "client_secret": "changeme-google-client-secret",
    "redirect_uri": "https://example.invalid/api/v1/auth/google/callback"
  },

  "mail": {
    "mailer": "smtp",
    "host": "smtp.example.invalid",
    "port": "587",
    "encryption": "tls",
    "username": "changeme-smtp-username",
    "password": "changeme-smtp-password",
    "from_address": "no-reply@example.invalid",
    "from_name": "Example Marketplace"
  },

  "anthropic": {
    "api_key": "changeme-anthropic-api-key",
    "default_model": "claude-sonnet-5",
    "enabled": "true"
  },

  "slack": {
    "webhook_url": "https://hooks.slack.example.invalid/services/changeme",
    "enabled": "false"
  }
}
