# Development and CI only. Never installed in a deployed image.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate from requirements.txt so a production image carries no dev
# tooling — fewer packages to audit and to keep patched. Note this is NOT what
# keeps a test client out of production: FastAPI's TestClient comes from
# starlette, which is a runtime dependency and is always present.
-r requirements.txt

pytest==8.3.4
# `httpx` is already a runtime dependency (Google token exchange), and FastAPI's
# TestClient is built on it — so API tests need no additional HTTP library.
ruff==0.8.6
